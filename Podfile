# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'bleexplorer' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for bleexplorer

  target 'bleexplorerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'bleexplorerUITests' do
    # Pods for testing
  end

end
